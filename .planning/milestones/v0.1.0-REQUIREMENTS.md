# Requirements Archive — v0.1.0: Complete Core CLI

All 40 requirements delivered.

## R1: CLI Validate Command
- [x] **R1.1** `dmjedi validate <paths>` parses all `.dv` files and runs the linter
- [x] **R1.2** Accepts both file paths and directories (recursively discovers `.dv` files)
- [x] **R1.3** Prints lint diagnostics with severity, source file, line, column, and rule ID
- [x] **R1.4** Exits with non-zero status if any ERROR-level diagnostics found
- [x] **R1.5** Displays user-friendly parse error messages with source context

## R2: CLI Generate Command
- [x] **R2.1** `dmjedi generate <paths> --target <name> --output <dir>` runs the full pipeline
- [x] **R2.2** Validates input first (parse + lint); aborts on errors with diagnostics
- [x] **R2.3** Resolves all modules into a single DataVaultModel
- [x] **R2.4** Invokes the named generator and writes output files
- [x] **R2.5** Lists available generators if an unknown target is specified
- [x] **R2.6** Reports what files were generated (count + paths)

## R3: CLI Docs Command
- [x] **R3.1** `dmjedi docs <paths> --output <dir>` generates markdown documentation
- [x] **R3.2** Validates input first; aborts on errors
- [x] **R3.3** Writes documentation files to the output directory

## R4: Multi-File & Import Support
- [x] **R4.1** Resolver handles `import "path"` declarations — recursively loads imported files
- [x] **R4.2** Import paths resolved relative to the importing file's directory
- [x] **R4.3** Circular imports detected and reported as errors
- [x] **R4.4** File/directory discovery: CLI recursively finds `.dv` files in directory arguments

## R5: Error Reporting
- [x] **R5.1** Parse errors formatted with source file, line, column, and a context snippet
- [x] **R5.2** Lint diagnostics show severity icon, location, message, and rule ID
- [x] **R5.3** Resolver errors (duplicate entities, missing parent refs) reported with locations
- [x] **R5.4** Rich terminal output with color when stdout is a TTY

## R6: Resolver Improvements
- [x] **R6.1** Duplicate entity names raise an error listing both source locations
- [x] **R6.2** Satellite `parent_ref` validated against known hubs and links
- [x] **R6.3** Namespace validation: warn if no namespace declared

## R7: Generator Quality — SQL Jinja
- [x] **R7.1** Fix trailing comma bug in link template
- [x] **R7.2** Add data type mapping from DVML types to SQL types (configurable per dialect)
- [x] **R7.3** Generate standard DV2.1 columns (hash keys, load_date, record_source) automatically
- [x] **R7.4** SQL output is syntactically valid

## R8: Generator Quality — Spark Declarative
- [x] **R8.1** Generate functional DLT Python code (not `pass` stubs)
- [x] **R8.2** Include `@dlt.table` decorators with correct table names
- [x] **R8.3** Generate hash key computation logic
- [x] **R8.4** Generate standard DV2.1 columns (hash keys, load_date, record_source)

## R9: Testing
- [x] **R9.1** CLI commands tested via Typer test client
- [x] **R9.2** Import resolution tested with multi-file fixtures
- [x] **R9.3** Error formatting tested (parse errors, lint diagnostics, resolver errors)
- [x] **R9.4** Generator output validated against expected baselines (snapshot tests)
- [x] **R9.5** Integration test: full pipeline from `.dv` file to generated output

## R10: Documentation
- [x] **R10.1** README updated with working CLI usage examples
- [x] **R10.2** Example `.dv` files demonstrate all supported features
