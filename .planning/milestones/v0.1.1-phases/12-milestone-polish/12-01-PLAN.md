---
phase: 12-milestone-polish
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/dmjedi/lang/linter.py
  - src/dmjedi/cli/main.py
  - tests/test_linter.py
  - tests/test_cli.py
autonomous: true
requirements:
  - LINT-03

must_haves:
  truths:
    - "_check_naming() in linter.py produces naming-convention warnings for bridge and pit entities when a prefix is configured"
    - "A bridge entity name that violates a configured prefix triggers a naming-convention diagnostic"
    - "A pit entity name that violates a configured prefix triggers a naming-convention diagnostic"
    - "dmjedi docs exits with code 1 when model-aware diagnostics contain errors (consistent with validate/generate)"
    - "dmjedi docs exits with code 0 when model-aware diagnostics are warnings only"
  artifacts:
    - path: src/dmjedi/lang/linter.py
      provides: _check_naming with 9 entity types
      contains: '"bridge", module.bridges'
    - path: src/dmjedi/lang/linter.py
      provides: _check_naming with 9 entity types
      contains: '"pit", module.pits'
    - path: src/dmjedi/cli/main.py
      provides: docs error gate
      contains: model_aware_error_count
    - path: tests/test_linter.py
      provides: test coverage for bridge/pit naming checks
    - path: tests/test_cli.py
      provides: test for docs exit code 1 on model-aware errors
  key_links:
    - from: src/dmjedi/lang/linter.py
      to: tests/test_linter.py
      via: _check_naming checks list
      pattern: '"bridge".*module\.bridges'
    - from: src/dmjedi/cli/main.py
      to: tests/test_cli.py
      via: docs command model_aware_error_count guard
      pattern: "model_aware_error_count"
---

<objective>
Close the two tech debt items from the v0.1.1 milestone audit:

1. LINT-03 bridge/pit gap — `_check_naming()` in `linter.py` currently checks 7/9 entity types.
   Bridge and pit are omitted. Adding them ensures users who configure bridge/pit naming prefixes
   in `.dvml-lint.toml` actually get warnings.

2. docs command error gate — `dmjedi docs` runs model-aware lint (LINT-01 effsat-parent check)
   but never exits with code 1 even when that lint finds errors. The `validate` and `generate`
   commands already have this guard; `docs` needs the same pattern for behavioral consistency.

Purpose: Complete LINT-03 coverage across all 9 DV 2.1 entity types and make CLI error handling
consistent across all three data-producing commands.

Output: Updated linter.py (9-type checks list), updated main.py (docs error gate), updated tests.
</objective>

<execution_context>
@/Users/kevin_haferkamp/Projects/dmjedi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kevin_haferkamp/Projects/dmjedi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend _check_naming() to cover bridge and pit (LINT-03)</name>
  <files>src/dmjedi/lang/linter.py, tests/test_linter.py</files>

  <read_first>
    - src/dmjedi/lang/linter.py lines 162-195 — current _check_naming() implementation
    - tests/test_linter.py lines 227-253 — existing test_naming_all_seven_types test to understand pattern
    - src/dmjedi/lang/ast.py lines 101-133 — BridgeDecl, PitDecl, and DVMLModule.bridges / DVMLModule.pits
  </read_first>

  <behavior>
    - test_naming_bridge_missing_prefix: DVMLModule with one BridgeDecl named "MyBridge" and
      config `bridge = "br_"` → lint produces 1 diagnostic with rule "naming-convention" and
      message containing "does not start with required prefix 'br_'"
    - test_naming_pit_missing_prefix: DVMLModule with one PitDecl(name="MyPit", anchor_ref="H")
      and config `pit = "pit_"` → lint produces 1 diagnostic with rule "naming-convention" and
      message containing "does not start with required prefix 'pit_'"
    - test_naming_bridge_correct_prefix: BridgeDecl named "br_Sales" with config `bridge = "br_"` →
      lint produces 0 naming-convention diagnostics
    - test_naming_pit_correct_prefix: PitDecl named "pit_Sales" with config `pit = "pit_"` →
      lint produces 0 naming-convention diagnostics
    - test_naming_all_nine_types: Update the existing test_naming_all_seven_types to cover 9 types:
      add `bridge = "br_"` and `pit = "pit_"` to toml config, add BridgeDecl(name="X") and
      PitDecl(name="X", anchor_ref="H") to the module, assert len(diags) == 9
  </behavior>

  <action>
    RED phase — write failing tests first in tests/test_linter.py:

    1. Add four new test functions after test_naming_all_seven_types (around line 254):
       - test_naming_bridge_missing_prefix
       - test_naming_pit_missing_prefix
       - test_naming_bridge_correct_prefix
       - test_naming_pit_correct_prefix
    2. Update test_naming_all_seven_types: rename to test_naming_all_nine_types (or add new test),
       add bridge and pit entries to the toml string, add BridgeDecl(name="X", path=[]) and
       PitDecl(name="X", anchor_ref="H") to the module, change the assertion to `assert len(diags) == 9`
    3. Verify imports at top of test_linter.py include BridgeDecl and PitDecl from dmjedi.lang.ast
    4. Run `pytest tests/test_linter.py -k "naming" -x` — tests MUST fail (RED)

    GREEN phase — fix linter.py:

    5. In src/dmjedi/lang/linter.py, locate the `checks` list in `_check_naming()` (lines ~169-177)
    6. Append two entries to the list:
       ("bridge", module.bridges),
       ("pit", module.pits),
    7. Run `pytest tests/test_linter.py -k "naming" -x` — all tests MUST pass (GREEN)
    8. Run `pytest tests/test_linter.py` — all linter tests MUST pass, no regressions

    The message format uses `entity_type.capitalize()` so bridge→"Bridge" and pit→"Pit" automatically.
    No other changes to _check_naming() are needed.
  </action>

  <verify>
    <automated>cd /Users/kevin_haferkamp/Projects/dmjedi && pytest tests/test_linter.py -x -q</automated>
  </verify>

  <acceptance_criteria>
    - grep-check: `grep -n '"bridge", module.bridges' src/dmjedi/lang/linter.py` returns a match
    - grep-check: `grep -n '"pit", module.pits' src/dmjedi/lang/linter.py` returns a match
    - grep-check: `grep -n 'test_naming_bridge_missing_prefix\|test_naming_pit_missing_prefix' tests/test_linter.py` returns 2 matches
    - pytest: `pytest tests/test_linter.py -k "naming" -q` exits 0 with all naming tests passing
    - pytest: `pytest tests/test_linter.py -q` exits 0 (no regressions in linter test suite)
  </acceptance_criteria>

  <done>
    _check_naming() covers 9 entity types. bridge and pit naming violations produce
    "naming-convention" diagnostics. test_naming_all_nine_types asserts 9 diagnostics.
    All linter tests pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add model-aware error gate to docs command</name>
  <files>src/dmjedi/cli/main.py, tests/test_cli.py</files>

  <read_first>
    - src/dmjedi/cli/main.py lines 159-201 — full docs() command
    - src/dmjedi/cli/main.py lines 55-69 — validate() model_aware_error_count pattern to replicate exactly
    - tests/test_cli.py lines 82-92 — existing test_docs_valid test
  </read_first>

  <behavior>
    - test_docs_exits_on_model_aware_error: docs command invoked on a .dv file where an effsat
      references a hub (not a link) → exit code 1. This mirrors the validate/generate behavior.
    - test_docs_passes_on_warnings_only: docs command on a valid file with only warnings
      (not errors) → exit code 0, model.md is written.
  </behavior>

  <action>
    RED phase — write failing test first in tests/test_cli.py:

    1. After test_docs_valid (around line 92), add test_docs_exits_on_model_aware_error:
       - Use tmp_path to write a .dv file with an effsat referencing a hub (not a link),
         e.g.:
         ```
         namespace test
         hub Customer { business_key id: int }
         effsat CustomerStatus of Customer { }
         ```
       - Invoke: runner.invoke(app, ["docs", str(dv_file), "--output", str(tmp_path / "out")])
       - Assert result.exit_code == 1
    2. Run `pytest tests/test_cli.py -k "docs" -x` — the new test MUST fail (RED)
       (currently docs does not gate on model_aware_error_count, so exit code is 0)

    GREEN phase — fix main.py:

    3. In src/dmjedi/cli/main.py, locate the docs() function at line ~159
    4. After the `if model_aware_diags:` block (line ~192-194), add:
       ```python
       model_aware_error_count = sum(1 for d in model_aware_diags if d.severity == Severity.ERROR)
       if model_aware_error_count > 0:
           raise typer.Exit(code=1)
       ```
    5. Insert these 3 lines BEFORE the `markdown = generate_markdown(model)` call at line ~196
       so that docs are never written when errors are present
    6. Run `pytest tests/test_cli.py -k "docs" -x` — all docs tests MUST pass (GREEN)
    7. Run `pytest tests/test_cli.py -q` — full CLI test suite MUST pass, no regressions
    8. Run `pytest -q` — full test suite MUST pass
  </action>

  <verify>
    <automated>cd /Users/kevin_haferkamp/Projects/dmjedi && pytest tests/test_cli.py -x -q</automated>
  </verify>

  <acceptance_criteria>
    - grep-check: `grep -n 'model_aware_error_count' src/dmjedi/cli/main.py` returns a match inside docs()
    - grep-check: `grep -n 'test_docs_exits_on_model_aware_error' tests/test_cli.py` returns a match
    - pytest: `pytest tests/test_cli.py -k "docs" -q` exits 0 with all docs tests passing
    - pytest: `pytest -q` exits 0 (full test suite, no regressions across 195+ tests)
    - behavioral: invoking docs on an effsat-referencing-hub .dv file exits with code 1
    - behavioral: invoking docs on a valid .dv file still exits with code 0 and writes model.md
  </acceptance_criteria>

  <done>
    docs command exits with code 1 when model_aware_error_count > 0. Behavior matches
    validate and generate. New test confirms exit code 1. Full test suite passes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user config → linter | .dvml-lint.toml is user-supplied; _load_lint_config reads it with tomllib (safe, stdlib) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering | .dvml-lint.toml | accept | File is user-authored config for their own project; no privilege boundary crossed |
| T-12-02 | Denial of Service | linter _check_naming | accept | Iterates over user-supplied entity lists; bounded by file size already controlled by user |
</threat_model>

<verification>
After both tasks complete:

1. `pytest -q` — full suite passes (no regressions in 195+ tests)
2. `grep '"bridge", module.bridges' src/dmjedi/lang/linter.py` — match in checks list
3. `grep '"pit", module.pits' src/dmjedi/lang/linter.py` — match in checks list
4. `grep 'model_aware_error_count' src/dmjedi/cli/main.py | grep -c model_aware` — returns 3 (validate, generate, docs each have one)
5. `ruff check src/ tests/` — no lint errors
6. `mypy src/` — no type errors
</verification>

<success_criteria>
- `_check_naming()` covers all 9 entity types (hub, sat, link, nhsat, nhlink, effsat, samlink, bridge, pit)
- A .dvml-lint.toml with `bridge = "br_"` or `pit = "pit_"` now produces naming-convention warnings
- `dmjedi docs` exits with code 1 when model-aware lint finds errors, matching validate/generate behavior
- All existing tests continue to pass
- LINT-03 requirement fully satisfied (was partial at 7/9 types)
</success_criteria>

<output>
After completion, create `.planning/phases/12-milestone-polish/12-01-SUMMARY.md`
</output>
