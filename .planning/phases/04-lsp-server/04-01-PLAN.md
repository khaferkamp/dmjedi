---
phase: 04-lsp-server
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/dmjedi/lsp/analysis.py
  - src/dmjedi/lsp/protocol.py
  - src/dmjedi/lsp/server.py
  - src/dmjedi/cli/main.py
  - tests/test_lsp.py
  - tests/test_cli.py
autonomous: true
requirements:
  - LSP-01
must_haves:
  truths:
    - "Opening or editing a `.dv` file causes the server to publish parse and lint diagnostics for that current document."
    - "`dmjedi lsp` starts a real STDIO language server instead of exiting as unimplemented."
    - "The first LSP release keeps analysis scoped to the active document only."
  artifacts:
    - path: "src/dmjedi/lsp/analysis.py"
      provides: "Current-document parse and lint pipeline with cached analysis output"
    - path: "src/dmjedi/lsp/protocol.py"
      provides: "Mapping from parser/linter results to `lsprotocol.types` diagnostics and ranges"
    - path: "src/dmjedi/lsp/server.py"
      provides: "pygls language server bootstrap and didOpen/didChange diagnostic publishing"
    - path: "tests/test_lsp.py"
      provides: "Automated coverage for diagnostics and server-facing helpers"
  key_links:
    - from: "src/dmjedi/lsp/server.py"
      to: "src/dmjedi/lsp/analysis.py"
      via: "didOpen/didChange handlers"
      pattern: "analy"
    - from: "src/dmjedi/lsp/protocol.py"
      to: "src/dmjedi/lang/parser.py"
      via: "DVMLParseError to Diagnostic mapping"
      pattern: "Diagnostic"
    - from: "src/dmjedi/cli/main.py"
      to: "src/dmjedi/lsp/server.py"
      via: "lsp command entrypoint"
      pattern: "lsp"
---

<objective>
Replace the LSP stubs with a real single-document diagnostics server and a tested CLI entrypoint.

Purpose: Phase 04 cannot satisfy any editor behavior until `dmjedi lsp` boots a server and current-document diagnostics flow through parser/linter code already proven elsewhere in the repo.
Output: Real `pygls` server bootstrap, current-document analysis/protocol helpers, and regression tests for diagnostics plus CLI launch behavior.
</objective>

<execution_context>
@/Users/kevin_haferkamp/Projects/dmjedi/.codex/get-shit-done/workflows/execute-plan.md
@/Users/kevin_haferkamp/Projects/dmjedi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-lsp-server/04-CONTEXT.md
@.planning/phases/04-lsp-server/04-RESEARCH.md
@.planning/phases/04-lsp-server/04-VALIDATION.md
@src/dmjedi/lsp/server.py
@src/dmjedi/cli/main.py
@src/dmjedi/lang/parser.py
@src/dmjedi/lang/linter.py
@src/dmjedi/lang/ast.py
@tests/test_cli.py
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/TESTING.md

<interfaces>
From `src/dmjedi/lang/parser.py`:

```python
def parse(source: str, source_file: str = "<string>") -> DVMLModule: ...
def parse_file(path: Path) -> DVMLModule: ...

class DVMLParseError(Exception):
    error: ParseError
```

From `src/dmjedi/lang/linter.py`:

```python
@dataclass
class LintDiagnostic:
    message: str
    severity: Severity
    loc: SourceLocation
    rule: str

def lint(
    module: DVMLModule,
    model: DataVaultModel | None = None,
    config_path: Path | None = None,
) -> list[LintDiagnostic]: ...
```

From `src/dmjedi/lang/ast.py`:

```python
class SourceLocation(BaseModel):
    file: str = ""
    line: int = 0
    column: int = 0

class DVMLModule(BaseModel):
    namespace: str = ""
    source_file: str = ""
```

Executors should build LSP ranges from live document text plus these start-only locations; do not change parser architecture or expand to workspace indexing.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the single-document analysis and diagnostic mapping layer</name>
  <read_first>
    @src/dmjedi/lang/parser.py
    @src/dmjedi/lang/linter.py
    @src/dmjedi/lang/ast.py
    @.planning/phases/04-lsp-server/04-CONTEXT.md
    @.planning/phases/04-lsp-server/04-RESEARCH.md
  </read_first>
  <files>src/dmjedi/lsp/analysis.py, src/dmjedi/lsp/protocol.py, tests/test_lsp.py</files>
  <acceptance_criteria>
    - Parse failures map into `lsprotocol.types.Diagnostic` objects with the correct start position and a stable range derived from current document text.
    - Lint diagnostics from the current document map into LSP diagnostics without shelling out through the CLI.
    - The analysis result stays current-document-only per D-01, D-02, D-03, and D-04; no import graph or workspace cache is introduced.
  </acceptance_criteria>
  <action>Create `src/dmjedi/lsp/analysis.py` around a per-document analysis result that stores URI, version, source text, parsed module when available, and LSP diagnostics generated from existing parser/linter calls. Create `src/dmjedi/lsp/protocol.py` to hold reusable range helpers and typed `lsprotocol.types` conversion functions for `DVMLParseError`, `LintDiagnostic`, and `SourceLocation`, explicitly reconstructing end columns from live text because `SourceLocation` only stores line/column starts. Add focused tests in `tests/test_lsp.py` that lock the diagnostic mapping behavior before server handlers use it. Keep scope current-document only per D-01 through D-04 and do not add resolver-driven workspace behavior or duplicate parser logic.</action>
  <verify>
    <automated>uv run pytest tests/test_lsp.py -k diagnostics -x</automated>
  </verify>
  <done>The repo has a reusable current-document analysis pipeline and tests proving parse/lint diagnostics map cleanly into LSP diagnostics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace the LSP stubs with a real STDIO diagnostics server</name>
  <read_first>
    @src/dmjedi/lsp/server.py
    @src/dmjedi/cli/main.py
    @tests/test_cli.py
    @src/dmjedi/lsp/analysis.py
    @src/dmjedi/lsp/protocol.py
  </read_first>
  <files>src/dmjedi/lsp/server.py, src/dmjedi/cli/main.py, tests/test_lsp.py, tests/test_cli.py</files>
  <acceptance_criteria>
    - `src/dmjedi/lsp/server.py` exposes a real `pygls` server that publishes diagnostics on open/change for the active document per D-04.
    - `dmjedi lsp` starts the server rather than printing the current stub error.
    - Tests cover both the diagnostic publish path and the CLI entrypoint contract.
  </acceptance_criteria>
  <action>Replace the placeholder `src/dmjedi/lsp/server.py` with a `pygls` 2.x STDIO server that owns a per-URI analysis cache, refreshes analysis on `textDocument/didOpen` and `textDocument/didChange`, and publishes typed diagnostics from Task 1. Update `src/dmjedi/cli/main.py` so the `lsp` Typer command launches that server directly instead of exiting 1. Extend `tests/test_lsp.py` for handler-level diagnostic publication and add or update `tests/test_cli.py` to prove the CLI no longer reports “not yet implemented.” Keep the command local-only and avoid daemon/process-management complexity; v0.2.0 is a simple editor-started STDIO server per D-01 and D-02.</action>
  <verify>
    <automated>uv run pytest tests/test_lsp.py tests/test_cli.py -k "diagnostics or lsp" -x</automated>
  </verify>
  <done>`dmjedi lsp` boots a real server, diagnostics publish from active-document parser/linter results, and the stub path is removed from both server and CLI behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| editor client → LSP server | Untrusted document text enters the server through open/change notifications |
| parser/linter results → LSP protocol objects | Incorrect range or severity mapping can hide real errors or mislead the editor |
| CLI entrypoint → long-running server process | The command must start the intended server path without shelling out or using stale stub behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | T | `src/dmjedi/lsp/protocol.py` | mitigate | Convert parser/linter results through one typed mapping layer and test diagnostic ranges against known source text |
| T-04-02 | D | `src/dmjedi/lsp/server.py` didOpen/didChange flow | mitigate | Keep analysis current-document-only with lightweight per-URI cache refreshes instead of resolver/workspace passes |
| T-04-03 | R | `src/dmjedi/cli/main.py` lsp command | mitigate | Replace the explicit stub exit with a direct server launch and verify it in CLI tests |
</threat_model>

<verification>
Run `uv run pytest tests/test_lsp.py tests/test_cli.py -k "diagnostics or lsp" -x`.

## Source Audit

| Source Type | Item | Coverage |
|-------------|------|----------|
| GOAL | Working LSP server for diagnostics, completions, hover, definition, and symbols | 04-01 builds the server bootstrap and diagnostics foundation that 04-02 and 04-03 extend |
| REQ | LSP-01 | 04-01 |
| REQ | LSP-02, LSP-03 | 04-02 |
| REQ | LSP-04, LSP-05, LSP-06 | 04-03 |
| RESEARCH | pygls 2.x STDIO server with current-document analysis cache | 04-01, 04-02, 04-03 |
| RESEARCH | range reconstruction from live text instead of parser changes | 04-01 and reused downstream |
| CONTEXT | D-01, D-02, D-03 single-document/simple-first scope | enforced across all three plans |
| CONTEXT | D-04 diagnostics on current document in real time | 04-01 |
| CONTEXT | D-05 conservative completions | 04-02 |
| CONTEXT | D-06 current-document hover/definition only | 04-03 |
| CONTEXT | D-07 document symbols for current file only | 04-03 |
| CONTEXT | D-08, D-09 defer workspace-aware follow-up | excluded from every plan |
</verification>

<success_criteria>
Phase 04 has a tested diagnostics foundation: current-document analysis exists, diagnostics publish through a real `pygls` server, and `dmjedi lsp` is no longer a stub.
</success_criteria>

<output>
After completion, create `.planning/phases/04-lsp-server/04-01-SUMMARY.md`
</output>
