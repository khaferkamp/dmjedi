---
phase: 04-lsp-server
plan: "03"
type: execute
wave: 3
depends_on:
  - 04-02
files_modified:
  - src/dmjedi/lsp/analysis.py
  - src/dmjedi/lsp/protocol.py
  - src/dmjedi/lsp/server.py
  - tests/test_lsp.py
autonomous: true
requirements:
  - LSP-04
  - LSP-05
  - LSP-06
must_haves:
  truths:
    - "Hover over a declaration or same-document reference shows useful entity details."
    - "Go-to-definition navigates to the declaration in the current document."
    - "Document symbols outline the entities in the open `.dv` file."
  artifacts:
    - path: "src/dmjedi/lsp/analysis.py"
      provides: "Same-document symbol and reference lookup helpers"
    - path: "src/dmjedi/lsp/protocol.py"
      provides: "Hover, Location, and DocumentSymbol range construction helpers"
    - path: "src/dmjedi/lsp/server.py"
      provides: "hover/definition/documentSymbol handlers"
    - path: "tests/test_lsp.py"
      provides: "Automated semantic LSP coverage and final phase gate assertions"
  key_links:
    - from: "src/dmjedi/lsp/server.py"
      to: "src/dmjedi/lsp/protocol.py"
      via: "hover/definition/symbol response builders"
      pattern: "Hover|Location|DocumentSymbol"
    - from: "src/dmjedi/lsp/analysis.py"
      to: "src/dmjedi/lang/ast.py"
      via: "declaration and reference lookup"
      pattern: "Decl"
---

<objective>
Finish the semantic editor features for Phase 04: hover, go-to-definition, and document symbols, then close the phase with automated verification.

Purpose: With diagnostics and completion in place, the remaining requirements all depend on the same-document symbol/range helpers and should ship together with the full LSP regression gate.
Output: Current-document hover, definition, and symbol handlers plus a final automated LSP test sweep.
</objective>

<execution_context>
@/Users/kevin_haferkamp/Projects/dmjedi/.codex/get-shit-done/workflows/execute-plan.md
@/Users/kevin_haferkamp/Projects/dmjedi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-lsp-server/04-CONTEXT.md
@.planning/phases/04-lsp-server/04-RESEARCH.md
@.planning/phases/04-lsp-server/04-VALIDATION.md
@src/dmjedi/lsp/analysis.py
@src/dmjedi/lsp/protocol.py
@src/dmjedi/lsp/server.py
@src/dmjedi/lang/ast.py
@tests/test_lsp.py

<interfaces>
The analysis and protocol layers should already expose current-document diagnostics and completion support from 04-01/04-02. Extend those same types for semantic navigation instead of creating parallel AST walkers.

Expected additions in this plan:

```python
def lookup_symbol_at_position(analysis: DocumentAnalysis, position: types.Position) -> ...: ...
def build_hover(...) -> types.Hover | None: ...
def build_definition_location(...) -> types.Location | None: ...
def build_document_symbols(analysis: DocumentAnalysis) -> list[types.DocumentSymbol]: ...
```

Hover/definition/symbols must stay current-document-only per D-02, D-06, and D-07.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build same-document symbol lookup and protocol helpers</name>
  <read_first>
    @src/dmjedi/lsp/analysis.py
    @src/dmjedi/lsp/protocol.py
    @src/dmjedi/lang/ast.py
    @.planning/phases/04-lsp-server/04-CONTEXT.md
  </read_first>
  <files>src/dmjedi/lsp/analysis.py, src/dmjedi/lsp/protocol.py, tests/test_lsp.py</files>
  <acceptance_criteria>
    - The analysis layer can identify declarations and same-document references at a cursor position.
    - Protocol helpers can build hover content, definition locations, and `DocumentSymbol` trees with accurate ranges derived from active document text.
    - Tests cover at least one declaration/reference case for hover and definition, plus document-symbol structure for a representative `.dv` file.
  </acceptance_criteria>
  <action>Extend `src/dmjedi/lsp/analysis.py` with same-document lookup helpers that map cursor positions to declarations or references using the parsed AST and range reconstruction already introduced in 04-01. Extend `src/dmjedi/lsp/protocol.py` with typed builders for hover content, definition `Location`, and hierarchical `DocumentSymbol` results so the server handlers remain thin. Use declaration details already present in the AST to assemble hover content that includes attributes, relationships, and source location when available, per LSP-04 and D-06. Add tests in `tests/test_lsp.py` first to lock the range and lookup behavior before wiring handlers. Do not introduce resolver/workspace navigation; same-document only per D-02, D-06, and D-07.</action>
  <verify>
    <automated>uv run pytest tests/test_lsp.py -k "hover or definition or symbols" -x</automated>
  </verify>
  <done>Shared helpers exist for semantic navigation and symbol generation, with tests proving same-document lookup and range fidelity.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire hover, definition, and document symbols into the server and run the phase gate</name>
  <read_first>
    @src/dmjedi/lsp/server.py
    @src/dmjedi/lsp/analysis.py
    @src/dmjedi/lsp/protocol.py
    @tests/test_lsp.py
    @.planning/phases/04-lsp-server/04-VALIDATION.md
  </read_first>
  <files>src/dmjedi/lsp/server.py, tests/test_lsp.py</files>
  <acceptance_criteria>
    - `textDocument/hover`, `textDocument/definition`, and `textDocument/documentSymbol` all resolve from the active document cache.
    - The full LSP-focused automated checks in `04-VALIDATION.md` pass, and the plan also runs the repo’s full `uv run pytest` gate.
    - No part of the implementation expands into workspace-aware behavior or unresolved manual verification.
  </acceptance_criteria>
  <action>Add hover, definition, and document-symbol handlers to `src/dmjedi/lsp/server.py`, delegating to the shared analysis/protocol helpers so parsing is still centralized. Extend `tests/test_lsp.py` to exercise the server-facing request paths rather than only unit helpers, then run the full LSP test module followed by the repo-level `uv run pytest` gate defined in `04-VALIDATION.md`. Keep all behavior current-document-only and automated; Phase 04 explicitly avoids human-only validation and defers workspace-aware follow-ups per D-08 and D-09.</action>
  <verify>
    <automated>uv run pytest tests/test_lsp.py -x && uv run pytest</automated>
  </verify>
  <done>The server satisfies hover, go-to-definition, and document symbols for the active document, and the full repo test suite passes with the new LSP coverage in place.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cursor position → semantic lookup | Incorrect lookup can navigate users to the wrong declaration or show misleading hover text |
| AST/source locations → LSP ranges | Start-only source locations require careful range reconstruction to avoid highlighting the wrong spans |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | T | semantic lookup in `src/dmjedi/lsp/analysis.py` | mitigate | Resolve hover/definition only from same-document declarations and references derived from the cached AST, then test representative cursor positions |
| T-04-08 | I | `src/dmjedi/lsp/protocol.py` range builders | mitigate | Reuse one range helper layer for hover, definition, and symbol output so selection spans stay consistent across features |
| T-04-09 | R | Phase 04 verification gate | mitigate | Run both targeted LSP tests and the full `uv run pytest` suite before the phase is considered complete |
</threat_model>

<verification>
Run `uv run pytest tests/test_lsp.py -x` and then `uv run pytest`.
</verification>

<success_criteria>
All remaining LSP requirements are covered by automated tests, the server offers hover/definition/symbols for the active document, and the repo-level suite stays green.
</success_criteria>

<output>
After completion, create `.planning/phases/04-lsp-server/04-03-SUMMARY.md`
</output>
