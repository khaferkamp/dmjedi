-- Hub: {{ hub.name }}
-- Generated by DMJEDI

CREATE TABLE IF NOT EXISTS {{ hub.name }} (
    {{ hub.name }}_hk BINARY NOT NULL,
    load_ts TIMESTAMP NOT NULL,
    record_source {{ map_type("string") }} NOT NULL{% if hub.business_keys %},{% endif %}
{%- for bk in hub.business_keys %}
    {{ bk.name }} {{ map_type(bk.data_type) }}{% if not loop.last %},{% endif %}
{%- endfor %}
);
