-- Hub: {{ hub.name }}
-- Generated by DMJEDI

CREATE TABLE IF NOT EXISTS {{ hub.name }} (
    {{ hub.name }}_hk {{ map_type("hashkey") }} NOT NULL,
    load_ts {{ map_type("load_ts") }} NOT NULL,
    record_source {{ map_type("record_source") }} NOT NULL{% if hub.business_keys %},{% endif %}
{%- for bk in hub.business_keys %}
    {{ bk.name }} {{ map_type(bk.data_type) }}{% if not loop.last %},{% endif %}
{%- endfor %}
);
