-- Satellite: {{ sat.name }} (parent: {{ sat.parent_ref }})
-- Generated by DMJEDI

CREATE TABLE IF NOT EXISTS {{ sat.name }} (
    {{ sat.parent_ref }}_hk {{ map_type("hashkey") }} NOT NULL,
    load_ts {{ map_type("load_ts") }} NOT NULL,
    load_end_ts {{ map_type("load_ts") }},
    record_source {{ map_type("record_source") }} NOT NULL,
    hash_diff {{ map_type("hash_diff") }} NOT NULL{% if sat.columns %},{% endif %}
{%- for col in sat.columns %}
    {{ col.name }} {{ map_type(col.data_type) }}{% if not loop.last %},{% endif %}
{%- endfor %}
);
